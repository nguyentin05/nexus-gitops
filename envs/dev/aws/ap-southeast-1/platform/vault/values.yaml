server:
  serviceAccount:
    annotations:
      eks.amazonaws.com/role-arn: arn:aws:iam::065320271480:role/dev-vault-irsa
  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 1Gi

  dataStorage:
    size: 10Gi
    storageClass: gp2

  ha:
    replicas: 1
    raft:
      config: |
        ui = true

        listener "tcp" {
          tls_disable     = 1
          address         = "[::]:8200"
          cluster_address = "[::]:8201"

          telemetry {
            unauthenticated_metrics_access = "true"
          }
        }

        telemetry {
          prometheus_retention_time = "30s"
          disable_hostname          = true
        }

        storage "raft" {
          path = "/vault/data"
        }

        seal "awskms" {
          region     = "ap-southeast-1"
          kms_key_id = "alias/dev-vault-unseal"
        }

        service_registration "kubernetes" {}
