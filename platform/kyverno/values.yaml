admissionController:
  replicas: 1
  nodeSelector:
    kubernetes.io/os: linux
    role: system
  tolerations:
    - key: CriticalAddonsOnly
      operator: Exists
      effect: NoSchedule

backgroundController:
  replicas: 1
  nodeSelector:
    kubernetes.io/os: linux
    role: system
  tolerations:
    - key: CriticalAddonsOnly
      operator: Exists
      effect: NoSchedule

cleanupController:
  replicas: 1
  nodeSelector:
    kubernetes.io/os: linux
    role: system
  tolerations:
    - key: CriticalAddonsOnly
      operator: Exists
      effect: NoSchedule

reportsController:
  replicas: 1
  nodeSelector:
    kubernetes.io/os: linux
    role: system
  tolerations:
    - key: CriticalAddonsOnly
      operator: Exists
      effect: NoSchedule
